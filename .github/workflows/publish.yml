name: Release (tag + GitHub Release + PyPI)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write   # create tags + releases
  id-token: write   # PyPI trusted publishing

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0    # required for tags/history
          fetch-tags: true  # ensure tags are present

      - name: Get next version
        uses: reecetech/version-increment@2023.10.1
        id: version
        with:
          use_api: true

      - name: Create tag (if not exists)
        shell: bash
        run: |
          set -euxo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          TAG="v${VERSION}"

          # tags should already be available due to checkout settings, but this is safe
          git fetch --tags --force

          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists; exiting."
            exit 0
          fi

          git tag -a "$TAG" -m "$TAG"
          git push origin "$TAG"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euxo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          gh release create "v${VERSION}" --generate-notes

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Build
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
          python -m build

      - name: Publish to PyPI (Trusted Publishing)
        uses: pypa/gh-action-pypi-publish@release/v1